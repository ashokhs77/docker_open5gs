<?xml version="1.0" encoding="ISO-8859-1" ?>
<!--
  P-CSCF-routed conference dial (audio) — COMPLETE PATH.
  Sends INVITE sip:[service]@IMS_DOMAIN to the P-CSCF, where [service] is a
  conference room number 1NNR (e.g. -s 1016). The P-CSCF FSDISPATCH routes it to
  FreeSWITCH (real IMS conference path, NOT a direct FreeSWITCH dial) and the
  P-CSCF conference-CDR hook writes a conf_cdr.csv LEG row on the BYE.

  IMS_DOMAIN is substituted by the test helper before execution.
  Hold duration is the <pause/> with no value → controlled by SIPp `-d <ms>`,
  so the same scenario serves both quick single dials and long soak holds.
-->
<scenario name="P-CSCF Conference Dial (audio)">

  <send retrans="500">
    <![CDATA[
      INVITE sip:[service]@IMS_DOMAIN SIP/2.0
      Via: SIP/2.0/UDP [local_ip]:[local_port];branch=[branch]
      Max-Forwards: 70
      From: <sip:9876540700@IMS_DOMAIN>;tag=[call_number]
      To: <sip:[service]@IMS_DOMAIN>
      Call-ID: [call_id]
      CSeq: 1 INVITE
      Contact: <sip:9876540700@[local_ip]:[local_port]>
      Allow: INVITE,ACK,BYE,CANCEL,UPDATE,REFER,NOTIFY
      Content-Type: application/sdp
      Content-Length: [len]

      v=0
      o=sipp 1 1 IN IP4 [local_ip]
      s=Conference Call
      c=IN IP4 [local_ip]
      t=0 0
      m=audio 20000 RTP/AVP 96 8 0
      a=rtpmap:96 AMR-WB/16000
      a=rtpmap:8 PCMA/8000
      a=rtpmap:0 PCMU/8000
      a=sendrecv
    ]]>
  </send>

  <recv response="100" optional="true" timeout="5000"/>
  <recv response="180" optional="true" timeout="5000"/>
  <recv response="200" rtd="true" timeout="10000" rrs="true" />

  <send>
    <![CDATA[
      ACK [next_url] SIP/2.0
      Via: SIP/2.0/UDP [local_ip]:[local_port];branch=[branch]
      Max-Forwards: 70
      Route: [routes]
      From: <sip:9876540700@IMS_DOMAIN>;tag=[call_number]
      To: <sip:[service]@IMS_DOMAIN>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 1 ACK
      Content-Length: 0

    ]]>
  </send>

  <!-- Time spent in the conference; controlled by SIPp -d <ms> -->
  <pause/>

  <send retrans="500">
    <![CDATA[
      BYE [next_url] SIP/2.0
      Via: SIP/2.0/UDP [local_ip]:[local_port];branch=[branch]
      Max-Forwards: 70
      Route: [routes]
      From: <sip:9876540700@IMS_DOMAIN>;tag=[call_number]
      To: <sip:[service]@IMS_DOMAIN>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 2 BYE
      Content-Length: 0

    ]]>
  </send>

  <recv response="200" timeout="5000"/>

  <ResponseTimeRepartition value="10, 20, 30, 40, 50, 100, 150, 200"/>
  <CallLengthRepartition value="10, 50, 100, 500, 1000, 5000, 10000"/>
</scenario>
