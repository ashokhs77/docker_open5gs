<?xml version="1.0" encoding="ISO-8859-1" ?>
<!--
  UE-A Conference Factory Test (UAC):
  Sends INVITE sip:mmtel@conf-factory.IMS_DOMAIN to the P-CSCF.
  The IMS_DOMAIN placeholder is replaced by the test helper before execution.

  If we get 200 OK = P-CSCF correctly routed to FreeSWITCH = PASS
  If we get 4xx/5xx = routing failed = FAIL
-->
<scenario name="UE-A Conference Factory INVITE">

  <!-- Send INVITE to conf-factory -->
  <send retrans="500">
    <![CDATA[
      INVITE sip:mmtel@conf-factory.IMS_DOMAIN SIP/2.0
      Via: SIP/2.0/UDP [local_ip]:[local_port];branch=[branch]
      Max-Forwards: 70
      From: <sip:9876540700@IMS_DOMAIN>;tag=[call_number]
      To: <sip:mmtel@conf-factory.IMS_DOMAIN>
      Call-ID: [call_id]
      CSeq: 1 INVITE
      Contact: <sip:9876540700@[local_ip]:[local_port]>
      Allow: INVITE,ACK,BYE,CANCEL,UPDATE,REFER,NOTIFY
      Content-Type: application/sdp
      Content-Length: [len]

      v=0
      o=sipp 1 1 IN IP4 [local_ip]
      s=Merge Test Call
      c=IN IP4 [local_ip]
      t=0 0
      m=audio 20000 RTP/AVP 96 8 0
      a=rtpmap:96 AMR-WB/16000
      a=rtpmap:8 PCMA/8000
      a=rtpmap:0 PCMU/8000
      a=sendrecv
    ]]>
  </send>

  <!-- Expect 100 Trying (optional) -->
  <recv response="100" optional="true" timeout="5000"/>

  <!-- Expect 200 OK from FreeSWITCH (via P-CSCF) -->
  <recv response="200" rtd="true" timeout="10000" rrs="true" />

  <!-- Send ACK with Record-Route path -->
  <send>
    <![CDATA[
      ACK [next_url] SIP/2.0
      Via: SIP/2.0/UDP [local_ip]:[local_port];branch=[branch]
      Max-Forwards: 70
      Route: [routes]
      From: <sip:9876540700@IMS_DOMAIN>;tag=[call_number]
      To: <sip:mmtel@conf-factory.IMS_DOMAIN>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 1 ACK
      Content-Length: 0

    ]]>
  </send>

  <!-- Brief pause in conference -->
  <pause milliseconds="2000"/>

  <!-- Send BYE with Record-Route path -->
  <send retrans="500">
    <![CDATA[
      BYE [next_url] SIP/2.0
      Via: SIP/2.0/UDP [local_ip]:[local_port];branch=[branch]
      Max-Forwards: 70
      Route: [routes]
      From: <sip:9876540700@IMS_DOMAIN>;tag=[call_number]
      To: <sip:mmtel@conf-factory.IMS_DOMAIN>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 2 BYE
      Content-Length: 0

    ]]>
  </send>

  <!-- Expect 200 OK to BYE -->
  <recv response="200" timeout="5000"/>

  <ResponseTimeRepartition value="10, 20, 30, 40, 50, 100, 150, 200"/>
  <CallLengthRepartition value="10, 50, 100, 500, 1000, 5000, 10000"/>
</scenario>
