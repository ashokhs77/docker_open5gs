<?xml version="1.0" encoding="ISO-8859-1" ?>
<!--
  VoLTE RTP Echo Validation - UAC (Caller)
  Uses SIPp -rtp_echo to send real RTP packets and verify they loop back.
  The UAS side (volte_rtp_echo_uas.xml) must be started first.

  Run with:
    sipp PCSCF_IP:5060 -sf volte_rtp_echo_uac.xml -s CALLEE_MSISDN \
         -i LOCAL_IP -p 9300 -m 1 -l 1 -rtp_echo -timeout 30

  IMS_DOMAIN is replaced at runtime by run_sipp_templated().
-->
<scenario name="VoLTE RTP Echo UAC">

  <send retrans="500">
    <![CDATA[
      INVITE sip:[service]@IMS_DOMAIN SIP/2.0
      Via: SIP/2.0/UDP [local_ip]:[local_port];branch=[branch]
      Max-Forwards: 70
      From: <sip:9876540001@IMS_DOMAIN>;tag=[call_number]
      To: <sip:[service]@IMS_DOMAIN>
      Call-ID: [call_id]
      CSeq: 1 INVITE
      Contact: <sip:9876540001@[local_ip]:[local_port]>
      Allow: INVITE,ACK,BYE,CANCEL,OPTIONS,UPDATE
      Content-Type: application/sdp
      Content-Length: [len]

      v=0
      o=sipp [call_number] 1 IN IP4 [local_ip]
      s=VoLTE RTP Echo Test
      c=IN IP4 [local_ip]
      t=0 0
      m=audio [auto_media_port] RTP/AVP 8 0
      a=rtpmap:8 PCMA/8000
      a=rtpmap:0 PCMU/8000
      a=sendrecv
    ]]>
  </send>

  <recv response="100" optional="true" timeout="5000"/>
  <recv response="180" optional="true" timeout="5000"/>
  <recv response="183" optional="true" timeout="5000"/>
  <recv response="200" rtd="true" timeout="20000" rrs="true"/>

  <send>
    <![CDATA[
      ACK [next_url] SIP/2.0
      Via: SIP/2.0/UDP [local_ip]:[local_port];branch=[branch]
      Max-Forwards: 70
      Route: [routes]
      From: <sip:9876540001@IMS_DOMAIN>;tag=[call_number]
      To: <sip:[service]@IMS_DOMAIN>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 1 ACK
      Content-Length: 0

    ]]>
  </send>

  <!-- Hold for 3 seconds — rtp_echo will loop back any packets received -->
  <pause milliseconds="3000"/>

  <send retrans="500">
    <![CDATA[
      BYE [next_url] SIP/2.0
      Via: SIP/2.0/UDP [local_ip]:[local_port];branch=[branch]
      Max-Forwards: 70
      Route: [routes]
      From: <sip:9876540001@IMS_DOMAIN>;tag=[call_number]
      To: <sip:[service]@IMS_DOMAIN>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 2 BYE
      Content-Length: 0

    ]]>
  </send>

  <recv response="200" timeout="5000"/>

  <ResponseTimeRepartition value="10, 20, 50, 100, 200, 500, 1000, 2000"/>
  <CallLengthRepartition value="10, 50, 100, 500, 1000, 5000, 10000"/>
</scenario>
