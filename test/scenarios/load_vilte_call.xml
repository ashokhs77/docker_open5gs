<?xml version="1.0" encoding="ISO-8859-1" ?>
<!--
  ViLTE Load Test (UAC):
  Sustained video+audio call for capacity testing.
  Each call gets a unique extension (load_N) so there are NO conference
  member limits. FreeSWITCH answers and parks each call individually.

  Usage:
    sipp TARGET:PORT -sf load_vilte_call.xml -s load -r RATE -l LIMIT -m TOTAL
-->
<scenario name="ViLTE Load Test Call">

  <send retrans="500">
    <![CDATA[
      INVITE sip:load_[call_number]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/UDP [local_ip]:[local_port];branch=[branch]
      Max-Forwards: 70
      From: <sip:ue_[call_number]@[local_ip]>;tag=[call_number]
      To: <sip:load_[call_number]@[remote_ip]:[remote_port]>
      Call-ID: [call_id]
      CSeq: 1 INVITE
      Contact: <sip:ue_[call_number]@[local_ip]:[local_port]>
      Allow: INVITE,ACK,BYE,CANCEL,UPDATE
      Content-Type: application/sdp
      Content-Length: [len]

      v=0
      o=sipp [call_number] 1 IN IP4 [local_ip]
      s=ViLTE Load Test
      c=IN IP4 [local_ip]
      t=0 0
      m=audio [auto_media_port] RTP/AVP 96 8 0
      a=rtpmap:96 AMR-WB/16000
      a=rtpmap:8 PCMA/8000
      a=rtpmap:0 PCMU/8000
      a=sendrecv
      m=video 30000 RTP/AVP 97
      a=rtpmap:97 H264/90000
      a=fmtp:97 profile-level-id=42e01e;packetization-mode=1
      a=sendrecv
    ]]>
  </send>

  <recv response="100" optional="true" timeout="5000"/>
  <recv response="200" rtd="true" timeout="10000"/>

  <send>
    <![CDATA[
      ACK sip:load_[call_number]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/UDP [local_ip]:[local_port];branch=[branch]
      Max-Forwards: 70
      From: <sip:ue_[call_number]@[local_ip]>;tag=[call_number]
      To: <sip:load_[call_number]@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 1 ACK
      Content-Length: 0

    ]]>
  </send>

  <!-- Hold time: 5 seconds -->
  <pause milliseconds="5000"/>

  <send retrans="500">
    <![CDATA[
      BYE sip:load_[call_number]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/UDP [local_ip]:[local_port];branch=[branch]
      Max-Forwards: 70
      From: <sip:ue_[call_number]@[local_ip]>;tag=[call_number]
      To: <sip:load_[call_number]@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 2 BYE
      Content-Length: 0

    ]]>
  </send>

  <recv response="200" timeout="5000"/>

  <ResponseTimeRepartition value="10, 20, 30, 50, 100, 200, 500, 1000, 2000"/>
  <CallLengthRepartition value="10, 50, 100, 500, 1000, 5000, 10000, 30000"/>
</scenario>
