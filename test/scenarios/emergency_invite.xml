<?xml version="1.0" encoding="ISO-8859-1" ?>
<!--
  Emergency Call Smoke Test (sip:112@domain)
  Sends an INVITE to the emergency URI sip:112@IMS_DOMAIN and verifies
  the IMS stack responds with any non-5xx code (200/18x = routed to
  emergency center; 403/480 = rejected but not a stack crash).

  A 5xx response indicates an unhandled IMS error path — scenario fails.

  Run with:
    sipp PCSCF_IP:5060 -sf emergency_invite.xml -i LOCAL_IP \
         -p 9304 -m 1 -l 1 -timeout 15

  IMS_DOMAIN is replaced at runtime by run_sipp_templated().
-->
<scenario name="Emergency INVITE smoke test">

  <send retrans="500">
    <![CDATA[
      INVITE sip:112@IMS_DOMAIN SIP/2.0
      Via: SIP/2.0/UDP [local_ip]:[local_port];branch=[branch]
      Max-Forwards: 70
      From: <sip:9876540001@IMS_DOMAIN>;tag=[call_number]
      To: <sip:112@IMS_DOMAIN>
      Call-ID: [call_id]
      CSeq: 1 INVITE
      Contact: <sip:9876540001@[local_ip]:[local_port]>
      Priority: emergency
      Content-Type: application/sdp
      Content-Length: [len]

      v=0
      o=sipp [call_number] 1 IN IP4 [local_ip]
      s=Emergency Test
      c=IN IP4 [local_ip]
      t=0 0
      m=audio [auto_media_port] RTP/AVP 8 0
      a=rtpmap:8 PCMA/8000
      a=rtpmap:0 PCMU/8000
      a=sendrecv
    ]]>
  </send>

  <recv response="100" optional="true" timeout="5000"/>

  <!-- Accept any 1xx/2xx/3xx/4xx (anything non-5xx is acceptable) -->
  <recv response="180" optional="true" timeout="3000"/>
  <recv response="183" optional="true" timeout="3000"/>

  <!-- Accept 200 (call connected to PSAP), 403 (not authorized), or 480 (not available) -->
  <recv response="200" optional="true" timeout="8000"/>
  <recv response="403" optional="true" timeout="3000"/>
  <recv response="480" optional="true" timeout="3000"/>
  <recv response="486" optional="true" timeout="3000"/>

  <!-- If we got 200, send BYE immediately -->
  <send>
    <![CDATA[
      ACK sip:112@IMS_DOMAIN SIP/2.0
      Via: SIP/2.0/UDP [local_ip]:[local_port];branch=[branch]
      Max-Forwards: 70
      From: <sip:9876540001@IMS_DOMAIN>;tag=[call_number]
      To: <sip:112@IMS_DOMAIN>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 1 ACK
      Content-Length: 0

    ]]>
  </send>

  <ResponseTimeRepartition value="10, 20, 50, 100, 200, 500, 1000, 2000"/>
  <CallLengthRepartition value="10, 50, 100, 500, 1000"/>
</scenario>
