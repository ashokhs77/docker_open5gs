<?xml version="1.0" encoding="ISO-8859-1" ?>
<!--
  Single-conference SOAK leg (audio/VoLTE), UAC -> FreeSWITCH.
  Joins a conference room and STAYS for a long pause so that many legs can be
  held in ONE room simultaneously and membership measured via fs_cli. Uses
  [media_port] so each parallel SIPp instance (launched with its own -mp) gets a
  unique RTP port; run with -rtp_echo so the leg returns RTP and survives the
  FreeSWITCH rtp-timeout. The driver TC sends BYE by killing the process at the
  end of the soak window.

  Usage (per leg, parallel):
    sipp FS_IP:5090 -sf fs_conf_soak_audio.xml -s <ROOM> -i LOCAL_IP -p <SIP_PORT> -mp <RTP_PORT> -m 1 -l 1 -rtp_echo
-->
<scenario name="Conference soak leg (audio)">

  <send retrans="500">
    <![CDATA[
      INVITE sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/UDP [local_ip]:[local_port];branch=[branch]
      Max-Forwards: 70
      From: <sip:9876540700@[local_ip]>;tag=[call_number]
      To: <sip:[service]@[remote_ip]:[remote_port]>
      Call-ID: [call_id]
      CSeq: 1 INVITE
      Contact: <sip:9876540700@[local_ip]:[local_port]>
      Allow: INVITE,ACK,BYE,CANCEL,UPDATE
      Content-Type: application/sdp
      Content-Length: [len]

      v=0
      o=sipp 1 1 IN IP4 [local_ip]
      s=Conference Soak Audio
      c=IN IP4 [local_ip]
      t=0 0
      m=audio [media_port] RTP/AVP 8 0 96
      a=rtpmap:8 PCMA/8000
      a=rtpmap:0 PCMU/8000
      a=rtpmap:96 AMR-WB/16000
      a=sendrecv
    ]]>
  </send>

  <recv response="100" optional="true" timeout="5000"/>
  <recv response="180" optional="true" timeout="5000"/>
  <recv response="183" optional="true" timeout="5000"/>
  <recv response="200" rtd="true" timeout="10000"/>

  <send>
    <![CDATA[
      ACK sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/UDP [local_ip]:[local_port];branch=[branch]
      Max-Forwards: 70
      From: <sip:9876540700@[local_ip]>;tag=[call_number]
      To: <sip:[service]@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 1 ACK
      Content-Length: 0

    ]]>
  </send>

  <!-- Hold in the conference well past FreeSWITCH rtp-timeout (30s). The driver
       TC measures membership during this window and kills the leg at the end. -->
  <pause milliseconds="150000"/>

  <send retrans="500">
    <![CDATA[
      BYE sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/UDP [local_ip]:[local_port];branch=[branch]
      Max-Forwards: 70
      From: <sip:9876540700@[local_ip]>;tag=[call_number]
      To: <sip:[service]@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 2 BYE
      Content-Length: 0

    ]]>
  </send>

  <recv response="200" timeout="5000"/>

</scenario>
