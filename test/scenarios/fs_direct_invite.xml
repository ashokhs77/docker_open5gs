<?xml version="1.0" encoding="ISO-8859-1" ?>
<!--
  Direct FreeSWITCH Conference Test (UAC):
  Sends INVITE to sip:[service]@[remote_ip]:[remote_port]
  Tests that FreeSWITCH accepts calls to conference room numbers (4010, 5010).

  Usage:
    sipp FS_IP:5090 -sf fs_direct_invite.xml -s 4010 -i LOCAL_IP -p 7100 -m 1
-->
<scenario name="Direct FreeSWITCH Conference INVITE">

  <!-- Send INVITE to conference room number -->
  <send retrans="500">
    <![CDATA[
      INVITE sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/UDP [local_ip]:[local_port];branch=[branch]
      Max-Forwards: 70
      From: <sip:9876540700@[local_ip]>;tag=[call_number]
      To: <sip:[service]@[remote_ip]:[remote_port]>
      Call-ID: [call_id]
      CSeq: 1 INVITE
      Contact: <sip:9876540700@[local_ip]:[local_port]>
      Allow: INVITE,ACK,BYE,CANCEL,UPDATE
      Content-Type: application/sdp
      Content-Length: [len]

      v=0
      o=sipp 1 1 IN IP4 [local_ip]
      s=Conference Test
      c=IN IP4 [local_ip]
      t=0 0
      m=audio 20000 RTP/AVP 96 8 0
      a=rtpmap:96 AMR-WB/16000
      a=rtpmap:8 PCMA/8000
      a=rtpmap:0 PCMU/8000
      a=sendrecv
    ]]>
  </send>

  <!-- Expect 100 Trying (optional) -->
  <recv response="100" optional="true" timeout="5000"/>

  <!-- Expect 200 OK from FreeSWITCH -->
  <recv response="200" rtd="true" timeout="10000"/>

  <!-- Send ACK -->
  <send>
    <![CDATA[
      ACK sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/UDP [local_ip]:[local_port];branch=[branch]
      Max-Forwards: 70
      From: <sip:9876540700@[local_ip]>;tag=[call_number]
      To: <sip:[service]@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 1 ACK
      Content-Length: 0

    ]]>
  </send>

  <!-- Stay in conference briefly -->
  <pause milliseconds="2000"/>

  <!-- Send BYE -->
  <send retrans="500">
    <![CDATA[
      BYE sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/UDP [local_ip]:[local_port];branch=[branch]
      Max-Forwards: 70
      From: <sip:9876540700@[local_ip]>;tag=[call_number]
      To: <sip:[service]@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 2 BYE
      Content-Length: 0

    ]]>
  </send>

  <recv response="200" timeout="5000"/>

</scenario>
