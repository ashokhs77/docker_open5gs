<?xml version="1.0" encoding="ISO-8859-1" ?>
<!--
  FreeSWITCH Rejection Test (UAC):
  Sends INVITE to a conference room and expects a rejection response
  (486 Busy Here, 403 Forbidden, or 503 Service Unavailable).
  Used for max member enforcement testing.

  Usage:
    sipp FS_IP:5090 -sf fs_expect_reject.xml -s 5010 -i LOCAL_IP -p PORT -m 1
-->
<scenario name="FreeSWITCH Conference Rejection Test">

  <!-- Send INVITE to conference room -->
  <send retrans="500">
    <![CDATA[
      INVITE sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/UDP [local_ip]:[local_port];branch=[branch]
      Max-Forwards: 70
      From: <sip:9876540700@[local_ip]>;tag=[call_number]
      To: <sip:[service]@[remote_ip]:[remote_port]>
      Call-ID: [call_id]
      CSeq: 1 INVITE
      Contact: <sip:9876540700@[local_ip]:[local_port]>
      Allow: INVITE,ACK,BYE,CANCEL,UPDATE
      Content-Type: application/sdp
      Content-Length: [len]

      v=0
      o=sipp 1 1 IN IP4 [local_ip]
      s=Rejection Test
      c=IN IP4 [local_ip]
      t=0 0
      m=audio 20000 RTP/AVP 96 8 0
      a=rtpmap:96 AMR-WB/16000
      a=rtpmap:8 PCMA/8000
      a=rtpmap:0 PCMU/8000
      a=sendrecv
    ]]>
  </send>

  <!-- Expect 100 Trying (optional) -->
  <recv response="100" optional="true" timeout="5000"/>

  <!-- Expect a 4xx/5xx rejection (486 Busy, 403 Forbidden, 503 Unavailable) -->
  <!-- SIPp will accept ANY response code here -->
  <recv response="486" timeout="10000"/>

  <!-- Send ACK for the rejection -->
  <send>
    <![CDATA[
      ACK sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/UDP [local_ip]:[local_port];branch=[branch]
      Max-Forwards: 70
      From: <sip:9876540700@[local_ip]>;tag=[call_number]
      To: <sip:[service]@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 1 ACK
      Content-Length: 0

    ]]>
  </send>

</scenario>
