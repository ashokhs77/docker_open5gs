# Format: NIB_NUM:IP
# Lists every NIB's mmsc address. Used to (a) build one msmtp/mbuni relay
# account per remote NIB (mmsc_init.sh) and (b) fan out roaming-location
# updates to every other NIB (location_listener.py). No longer maps MSISDN
# ranges to a "home" NIB -- current location is tracked live and replicated
# to every NIB instead, so resolve_mmsc.sh no longer needs this file at all.
1:192.56.2.1
2:10.2.110.180
3:192.56.2.3
4:192.56.2.4
5:192.56.2.5
6:192.56.2.6
7:192.56.2.7
8:10.2.110.197
9:10.2.110.221
